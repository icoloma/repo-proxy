{
  "name": "test",
  "version": "0.0.1",
  "description": "Just enough to test that dependencies get downloaded correctly",
  "private": true,
  "dependencies": {
    "lodash": "^4.0.1"
  },
  "engines": {
    "node": ">=0.10.0",
    "npm": ">=2.1.18"
  }
}
